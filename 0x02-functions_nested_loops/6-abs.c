#include "main.h"

/**
 * _abs - make number positive
 * @n: The integer to be computed
 * Return: The abs value of the integer
 */
int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}
